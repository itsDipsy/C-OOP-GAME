#include <iostream>

void printHello();

void playerSpawn(); // This soon to be method spawns the number one(player in this case) into the gameMap(a multi dimmesional array consited of zeros to be the tiles where the player can move too);